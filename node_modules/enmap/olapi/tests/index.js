const { expect } = require('chai');
const Olapi = require('../');

describe("DataMapper Features", () => {
  const olapi = new Olapi();

  before(() => {
    olapi.checkAuth();
  });

  describe("General Features", () => {
    it("connects to the REST API", async () => {
      
    });
  });
});
